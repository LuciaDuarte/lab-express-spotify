<h1>Albums for: {{albums.0.artists.0.name}} </h1>
<div class="results">
  {{#each albums}}
    <span class="artists">
      <img src="{{images.[0].url}}" alt="{{name}}">
      <h4>{{name}}</h4>
      <a href="/tracks/{{id}}">View Tracks</a>
    </span>
  {{/each}}
</div>



